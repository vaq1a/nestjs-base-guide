doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Articles!
    body
        h1 #{title}
        ul
            each article in articles
                li #{article.title}  <a href="#{article.id}">Читать</a>
        a(href="create") Создать